# Urbex

```sh
cd flask-tutorial
flask --app flaskr run --debug
```

```sh
cd flask-tutorial
flask --app flaskr init-db
```

```sh
cd flask-tutorial
pip install -e .
```

```sh
printenv | grep PORT
python3 server.py
```